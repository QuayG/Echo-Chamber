import styled from 'styled-components/macro'

export default styled.main`
  display: grid;
  place-items: center;
  grid-gap: var(--size-xxl);
  padding: var(--size-xl);
  height: 100%;
  width: 100%;
  overflow-y: scroll;
`